# doc/txt/qto/generate-action-files/generate-action-files.func.txt

	doGenerateActionFiles
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doGenerateActionFiles function

	# Prerequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doGenerateActionFiles function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doGenerateActionFiles function to execute without errors

	# Interdependencies
	# ------------------------------------------------------
	-- todo: what are the interdependencies for the  
		doGenerateActionFiles function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doGenerateActionFiles used 

	# Development
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doGenerateActionFiles functions



# eof file: doc/txt/qto/generate-action-files/generate-action-files.func.txt
