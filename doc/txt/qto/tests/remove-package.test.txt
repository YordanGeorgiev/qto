# doc/txt/qto/remove-package/remove-package.test.txt

	doTestRemovePackage
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doTestRemovePackage function

	# Prerequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doTestRemovePackage function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doTestRemovePackage function to execute without errors

	# Interdependencies
	# ------------------------------------------------------
	-- todo: what are the interdependancies for the  
		doTestRemovePackage function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doTestRemovePackage used 

	# Development
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doTestRemovePackage functions



# eof file: doc/txt/qto/remove-package/remove-package.test.txt
