# doc/txt/qto/gmail-package/gmail-package.test.txt

	doTestGmailPackage
	
	# Purpose:
	# ------------------------------------------------------
	to test whether the sending to gmail works 

	# Prerequisites
	# ------------------------------------------------------
   preconfigured muttrc , mutt , see .muttrc from meta

	# Installation
	# ------------------------------------------------------
   register gmail account , install mutt , configure muttrc

	# Interdependencies
	# ------------------------------------------------------
   preconfigured muttrc , mutt , see .muttrc from meta

	# Usage:
	# ------------------------------------------------------
	bash src/bash/qto/qto.sh -a gmail-package

	# Development
	# ------------------------------------------------------
	-- simplify .muttrc configuration, add to to docs

	# Caveats:
	# ------------------------------------------------------
	- if there is not full-development package created no joy ..



# eof file: doc/txt/qto/gmail-package/gmail-package.test.txt
