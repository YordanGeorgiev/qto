# doc/txt/qto/generate-sql/generate-sql.test.txt

	doTestGenerateSQL
	
	# Purpose:
	# ------------------------------------------------------
	
	# Prerequisites
	# ------------------------------------------------------
   bash , perl , *.SQL file with %vars% notation in the 
   src/sql/mssql/run dir

	# Installation
	# ------------------------------------------------------
   unzip -o <<zip_pacckage>> -d <<dir-to-deploy>> 

	# Interdependencies
	# ------------------------------------------------------
   there should be a lst file in the dat/lst/mssql/list.list

	# Usage:
	# ------------------------------------------------------
   1. fill in the lst file 
	2. ensure tabs are used as token separators
   3. ensure you have a template -05-14 src/sql/mssql/run
   4. Action !!!
   bash src/bash/qto/qto.sh -a generate-sql
    

	# Development
	# ------------------------------------------------------
   
	# Caveats:
	# ------------------------------------------------------
   utf8 encoded templates are not supported ... ( yet ) 




# eof file: doc/txt/qto/generate-sql/generate-sql.test.txt
