# doc/txt/qto/clone-to-app/clone-to-app.test.txt

	doTestCloneToApp
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doTestCloneToApp function

	# Prerequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doTestCloneToApp function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doTestCloneToApp function to execute without errors

	# Interdependencies
	# ------------------------------------------------------
	-- todo: what are the interdependencies for the  
		doTestCloneToApp function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doTestCloneToApp used 

	# Development
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doTestCloneToApp functions



# eof file: doc/txt/qto/clone-to-app/clone-to-app.test.txt
