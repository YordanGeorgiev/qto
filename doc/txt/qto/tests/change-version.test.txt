# doc/txt/qto/change-version/change-version.test.txt

	doTestChangeVersion
	
	# Purpose:
	# ------------------------------------------------------
	-- todo: what is the purpose of the doTestChangeVersion function

	# Prerequisites
	# ------------------------------------------------------
	-- todo: what are the prerequisites of the doTestChangeVersion function

	# Installation
	# ------------------------------------------------------
	-- todo: what are the required installations for the  
		doTestChangeVersion function to execute without errors

	# Interdependencies
	# ------------------------------------------------------
	-- todo: what are the interdependencies for the  
		doTestChangeVersion function to execute without errors

	# Usage:
	# ------------------------------------------------------
	-- todo: how can be the doTestChangeVersion used 

	# Development
	# ------------------------------------------------------
	-- todo: what issues, todos could be pointed for development

	# Caveats:
	# ------------------------------------------------------
	-- todo: what are the known caveats of the doTestChangeVersion functions



# eof file: doc/txt/qto/change-version/change-version.test.txt
