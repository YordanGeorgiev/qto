# doc/txt/issue-tracker/func/curl-test-get-item.func.txt

	doCurlTestGetItem
	
	# Purpose:
	# ------------------------------------------------------
	to test the get single row from db as json hash with curl
   for a single table

	# Prequisites
	# ------------------------------------------------------
	the morbo web server up-and-running
   the morbo web_host and web_port configured
   a table populated with guid's

	# Installation
	# ------------------------------------------------------

	# Interdependancies
	# ------------------------------------------------------
	the morbo web server up-and-running
   the morbo web_host and web_port configured

	# Usage:
	# ------------------------------------------------------
   bash $0 -a curl-test-get-item

	# Developent
	# ------------------------------------------------------
   - fix docs 

	# Caveats:
	# ------------------------------------------------------
	if the hypnotoad web server is running the web port in the 
   product instance must be changed


# eof file: doc/txt/issue-tracker/func/curl-test-get-item.func.txt
